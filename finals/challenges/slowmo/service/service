service slowmoservice
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = slowmo
    bind        = 0.0.0.0
    server      = /home/slowmo/slowmo
    type        = UNLISTED
    port        = 31337
}

